<h1>Editing attendee</h1>

<%= render "form", attendee: @attendee %>

<br>

<div>
  <%= link_to "Show this attendee", @attendee %>
  <% if current_user.is_admin %>
    | <%= link_to "Back to attendees", attendees_path %>
  <% else %>
    <%= link_to "Back", root_path %>
  <% end %>
</div>
